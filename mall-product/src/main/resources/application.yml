server:
  port: 4001

spring:
  profiles:
    active: dev

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml  #扫描mapper文件
  global-config:
    db-config:
      id-type: auto #设置主键自增
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    com.yan.mall: debug